subscription TaskChangeStatusEvent($channel_id: ID!) {
  taskChangeStatusEvent(channel_id: $channel_id) {
    id
    room_id
    title
    voting_status_id
    closest_point
    avg_point
    counts_point
  }
}