mutation TaskCreate($room_id: ID!, $title: String!) {
  taskCreate(data: {
    room_id: $room_id,
    title: $title
  }) {
    id
    room_id
    title
    voting_status_id
    closest_point
    avg_point
    counts_point
  }
}
